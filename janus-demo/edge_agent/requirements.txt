# Janus Edge Agent Dependencies
# Updated March 2026 - RF-DETR + Supervision + Roboflow Trackers (All Apache 2.0 / MIT)

# Core Detection & Tracking
rfdetr                      # RF-DETR detection (Apache 2.0) - replaces ultralytics
trackers                    # ByteTrack/OC-SORT via Roboflow (Apache 2.0) - replaces boxmot
supervision>=0.25.0         # Zone counting, annotators, video utils (MIT)

# ML Runtime
torch>=2.0.0                # PyTorch runtime (BSD)
torchvision>=0.15.0         # Vision utilities
opencv-python>=4.10.0       # Image processing (Apache 2.0)
numpy>=1.24.0

# Intel Optimization (for Iris Xe iGPU inference)
openvino>=2025.4            # Intel-optimized inference runtime (Apache 2.0)
nncf                        # INT8 quantization for OpenVINO (Apache 2.0)

# Video streaming
yt-dlp>=2024.12.0           # YouTube stream extraction

# Web server
flask>=3.0.0
flask-cors>=4.0.0

# HTTP requests
requests>=2.31.0

# ============================================
# OPTIONAL: Enhanced Tracking (AGPL — Gate 4.1 integration)
# TrackerFactory falls back to ByteTrack gracefully if not installed.
# Install when BoT-SORT + ReID accuracy is required in dense scenes.
# ============================================
boxmot>=10.0.0            # BoT-SORT + CLIP-ReID (AGPL-3.0) — optional, graceful fallback to ByteTrack

# ============================================
# INSTALLATION INSTRUCTIONS
# ============================================
# Standard install (CPU + iGPU via OpenVINO):
#   pip install -r requirements.txt
#
# With NVIDIA GPU acceleration:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# Export RF-DETR to OpenVINO INT8 (for production speed on Intel Iris Xe):
#   python -c "from rfdetr import RFDETRNano; m = RFDETRNano(); m.export('rf_detr_nano.onnx')"
#   mo --input_model rf_detr_nano.onnx --output_dir openvino_model/
#   # Then quantize with nncf for INT8
